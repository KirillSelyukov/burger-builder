import React from 'react';
import './Toolbar.css';

export const Toolbar = (props) => (
    <header className='Toolbar'>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </header>
);